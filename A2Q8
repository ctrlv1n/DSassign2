#include <stdio.h>

int main(){
    int a[100],n,i,j,c=0,f;
    scanf("%d",&n);
    for(i=0;i<n;i++) scanf("%d",&a[i]);
    for(i=0;i<n;i++){
        f=1;
        for(j=0;j<i;j++) if(a[j]==a[i]){ f=0; break; }
        if(f) c++;
    }
    printf("%d",c);
}
